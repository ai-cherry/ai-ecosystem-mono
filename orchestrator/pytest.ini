[pytest]
# Mark tests as asyncio to handle async test functions
markers =
    asyncio: mark a test as an asyncio coroutine

# Search for tests in the tests directory
testpaths = tests

# Configure asyncio mode
asyncio_mode = auto

# Show test output for better debugging
verbose = true

# Fix PYTHONPATH to ensure imports work correctly
pythonpath = .
